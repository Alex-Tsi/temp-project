management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

camel:
  component:
    servlet:
      mapping:
        context-path: "/inherit/*"

server:
  port: 8080
